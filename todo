当输入拒绝理由后，点击回车应该直接发送，而不是需要再手动点击deny

当设置custome的provider的时候，最好有一个按钮来测试连接

可以编辑要运行的指令，例如git commit时修改commit描述

可以修改Edit，例如在编辑过程中，我可以接受一些行，拒绝一些，或者修改一些

workspace，agent可以和工程用同样的工作目录，如果一个工程里，有多个项目，则需要有多个workspace


在plan模式下，No, keep planning应该也能写反馈信息


在diff情况下，查看文件的时候，在查看整个文件的时候，要有一个上一个和下一个修改的按钮，便于查看修改
